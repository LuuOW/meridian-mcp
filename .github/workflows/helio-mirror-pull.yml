name: helio-mirror-pull

on:
  workflow_dispatch:
    inputs:
      perihelion:
        description: "Which perihelion window to pull (E20 / E21 / E22 / E23 / E24)"
        required: true
        default: "E20"
        type: choice
        options:
          - E20
          - E21
          - E22
          - E23
          - E24
      buckets:
        description: "Comma-separated buckets to pull (psp,probes,plasma,ephemeris,jwst) or 'all'"
        required: false
        default: "all"
        type: string

permissions:
  contents: read

jobs:
  pull:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: helio-mirror/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r helio-mirror/requirements.txt

      - name: Pull selected buckets
        working-directory: helio-mirror
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HELIO_PERIHELION: ${{ inputs.perihelion }}
          HELIO_PULL_BUCKETS: ${{ inputs.buckets }}
        run: python pull.py
